'use strict';

/**
 * course-collection service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::course-collection.course-collection');
